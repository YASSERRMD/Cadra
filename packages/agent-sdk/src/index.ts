/**
 * @cadra/agent-sdk
 *
 * Typed scene builder and text-to-scene interface for programmatic and
 * agent-driven authoring of Cadra scenes.
 *
 * This package is currently a placeholder scaffold. Framework logic lands
 * in a later phase.
 */

export const VERSION = "0.0.0";

/**
 * Identifies this package at runtime, useful for diagnostics until the
 * real typed builder and text-to-scene interface are implemented.
 */
export const PACKAGE_NAME = "@cadra/agent-sdk";
