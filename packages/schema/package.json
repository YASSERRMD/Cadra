{
  "name": "@cadra/schema",
  "version": "0.0.0",
  "private": false,
  "description": "Cadra schema package",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/generate-json-schema.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@cadra/core": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
