/**
 * @cadra/schema
 *
 * Zod-based scene DSL, JSON Schema generation, parser, and diagnostics for
 * the Cadra unified scene description format.
 *
 * This package is currently a placeholder scaffold. Framework logic lands
 * in a later phase.
 */

export const VERSION = "0.0.0";

/**
 * Identifies this package at runtime, useful for diagnostics until the
 * real DSL parser and schema definitions are implemented.
 */
export const PACKAGE_NAME = "@cadra/schema";
