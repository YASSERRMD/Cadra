/**
 * @cadra/encode
 *
 * WebCodecs-based frame capture, encoding, and muxing used to turn rendered
 * Cadra frames into deterministic MP4 output.
 *
 * This package is currently a placeholder scaffold. Framework logic lands
 * in a later phase.
 */

export const VERSION = "0.0.0";

/**
 * Identifies this package at runtime, useful for diagnostics until the
 * real capture, encode, and mux pipeline is implemented.
 */
export const PACKAGE_NAME = "@cadra/encode";
