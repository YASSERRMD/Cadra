/**
 * @cadra/player
 *
 * Live transport and preview for Cadra scenes: OffscreenCanvas worker
 * rendering and audio synchronization for in-browser WebGPU playback.
 *
 * This package is currently a placeholder scaffold. Framework logic lands
 * in a later phase.
 */

export const VERSION = "0.0.0";

/**
 * Identifies this package at runtime, useful for diagnostics until the
 * real transport and preview player are implemented.
 */
export const PACKAGE_NAME = "@cadra/player";
