/**
 * @cadra/headless
 *
 * Deterministic headless render and orchestration for Cadra scenes, driving
 * @cadra/renderer and @cadra/encode outside of a browser tab.
 *
 * This package is currently a placeholder scaffold. Framework logic lands
 * in a later phase.
 */

export const VERSION = "0.0.0";

/**
 * Identifies this package at runtime, useful for diagnostics until the
 * real headless render orchestration is implemented.
 */
export const PACKAGE_NAME = "@cadra/headless";
