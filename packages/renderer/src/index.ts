/**
 * @cadra/renderer
 *
 * Three.js WebGPU renderer and scene-graph reconciler that turns a Cadra
 * scene description into rendered frames.
 *
 * This package is currently a placeholder scaffold. Framework logic lands
 * in a later phase.
 */

export const VERSION = "0.0.0";

/**
 * Identifies this package at runtime, useful for diagnostics until the
 * real WebGPU renderer and reconciler are implemented.
 */
export const PACKAGE_NAME = "@cadra/renderer";
